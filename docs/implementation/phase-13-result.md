# Phase 13 Result — CRM-First Productization and Pilot Readiness

- **Date:** 2026-09-15
- **Gate:** Gate 13 — controlled-fixture closeout implemented; clean exact-head cumulative run pending
- **Accepted base:** Phase 12 / `694bd2f`
- **Hosted evidence source head:** `7275866` (`fix(phase-13): stabilize generated Sales authority hydration`)
- **Decision:** **REWORK — automated fixture readiness only; not a limited-beta approval**
- **Review state:** P13.1–P13.10 implementation, the bounded authority-projection, route-telemetry/hydration, and aggregate-ceiling repairs, their generated closure where applicable, and hosted run `34897020052`'s exact 33-file overlay passed persistent review. The latest P12.9 browser-lifecycle repair passes focused proof and awaits persistent review before another exact-head cumulative run.

## Scope delivered

Phase 13 turns the former Sales reference into one bounded CRM slice: daily account, contact, lead, opportunity, pipeline, activity, task, reminder, notification, import/export, communication, workflow, report, dashboard, upgrade, and restore behavior. `module.sales` remains the sole first-party domain module. It does not introduce a second vertical, public CMS, arbitrary query language, customer-authored workflow code, raw provider credentials, or dashboard-derived data authority.

## Completed task matrix

| Task | Delivered result | Focused evidence |
|---|---|---|
| P13.1 | Frozen CRM vocabulary, lifecycle, authority, metric, journey, attack, and kill-criterion contract. | `docs/adr/0028-crm-first-product-contract.md`; `contracts/phase-13-crm-product-contract.v1.json` |
| P13.2 | Canonical Account, Contact, Lead, Opportunity, Activity, Task, Note, Attachment, pipeline, permission, and exact Phase 12 cutover schema. | `fixtures/customer-gate-1/tests/p13-2-crm-core-migration-postgres.test.mjs` |
| P13.3 | Generated host routes/actions for CRM daily work, four persona authority/scopes, durable audit/outbox, and real browser journeys. | `p13-3-crm-http-postgres.test.mjs`; `p13-3-generated-crm-browser-postgres.test.mjs` |
| P13.4 | Configurable one-active-pipeline model, opaque UUIDv5 stages, Kanban, saved views, bounded custom-page embedding, and immutable stage-translation receipts. | `p13-4-pipeline-saved-views-postgres.test.mjs`; generated HTTP/Chromium pipeline suites |
| P13.5 | Closed CSV import/export, protected-field rejection, idempotent chunk recovery, dedupe/merge, and retained non-sensitive receipts. | `p13-5-data-movement-postgres.test.mjs`; `p13-5-generated-data-movement-browser-postgres.test.mjs` |
| P13.6 | Fixed email/calendar reference adapters, provider lifecycle, webhook receipts, reminders, and notifications without exposing credentials. | `p13-6-communications-postgres.test.mjs`; `p13-6-generated-communications-browser-postgres.test.mjs` |
| P13.7 | Compile-time three-rule workflow catalog, fenced worker execution, exactly-once logical effects, immutable evidence, and crash/replay isolation. | `p13-7-workflows-postgres.test.mjs`; `p13-7-generated-workflow-http-worker-postgres.test.mjs` |
| P13.8 | Seven fixed reports and dashboard blocks, exact decimal/currency/timezone execution metadata, queued delivery, and artifact authorization. | `p13-8-reports-postgres.test.mjs`; `p13-8-generated-reports-browser-postgres.test.mjs` |
| P13.9 | Exact Phase 12 predecessor migration chain, 100-Task/50-Opportunity stable-ID fixture, current-v1 package/lock attestation checks, forward-only maintenance boundary, physical clean restore, report artifact preservation, and web/worker restart. | `p13-9-upgrade-backup-restore-postgres.test.mjs`; `p13-9-generated-restore-browser-postgres.test.mjs` |
| P13.10 | Gate assembly, representative controlled fixture, 10,000-row import/replay, independent seven-metric ledger, reminder recovery, generated-host accessibility/performance/restore, atomic Task/genesis-audit creation, static lifecycle/authorization serialization, and fail-closed pilot decision. | `p13-10-limited-beta-fixture-postgres.test.mjs`; `p13-10-generated-fixture-browser-postgres.test.mjs`; `static-deployment-postgres.test.mjs`; exact-head cumulative run pending |

## Required journey and evidence matrix

| Gate 13 journey | Automated fixture coverage at this draft | Exact-head status |
|---|---|---|
| Generated application, real PostgreSQL, real Next/Payload HTTP, real Chromium | Generated CRM fixture builds a packed application, migrates PostgreSQL, boots web and worker, and drives Chromium. | P13.10 generated-host focused proof **PASS**; cumulative Gate 13 pending. |
| Representative and manager daily CRM work | P13.3 generated browser/HTTP suites cover account, contact, lead qualification, Opportunity, authority, audit, and non-enumeration. P13.10 runs representative and manager accounts across the daily route matrix on representative data. | P13.10 two-role route/accessibility proof **PASS**; cumulative Gate 13 pending. |
| Configured pipeline and Kanban | P13.4 migration/projection suites cover exactly six ordered opaque stages, saved-view CAS, Stage transition, immutable translation evidence, and Kanban. | **Pending** exact-head combined run. |
| Activity, task, reminder, notification | P13.3 activity/task journeys plus P13.6 communication/reminder and P13.7 workflow worker suites. | **Pending** one combined recovery proof at exact head. |
| Custom dashboard | P13.8 fixed report blocks and Phase 12 custom-page embedding prove page ACL cannot widen report/source authority. | **Pending** exact-head combined run. |
| Import/export | P13.5 PostgreSQL and generated browser tests cover closed CSV admission, diagnostics, replay, fenced workers, export authority, and merge. P13.10 executes 10,000 RFC4180 rows in 40 bounded chunks, crashes/reclaims at row 125, and exact-replays one logical result. | P13.10 focused PostgreSQL proof **PASS**; cumulative Gate 13 pending. |
| Communication adapter | P13.6 fixed reference provider and webhook/secret-boundary suites. | **Pending** exact-head combined run. |
| Reports | P13.8 executes seven fixed report definitions and queues real report artifacts; P13.9 preserves a completed artifact through clean restore. P13.10 reconciles all seven metrics to an independent test-owned ledger with zero money delta and a timezone-boundary fixture. | P13.10 focused PostgreSQL proof **PASS**; cumulative Gate 13 pending. |
| Backup/restore, version upgrade, restart | P13.9 runs the registered Phase 12 predecessor through all Phase 13 migrations, uses physical PostgreSQL restore into a distinct DB, reauthenticates, restarts generated web/worker, and downloads preserved report bytes. | Focused P13.9 tests passed on `80a08b7`; Gate 13 aggregate is **pending**. |
| Worker/realtime recovery | P13.5–P13.8 worker fencing/replay/crash suites plus P13.9 web/worker restart after restore. P13.10 proves stale/current/replay reminder delivery with one notification, two events, and authorized delivery within 60 seconds. | P13.10 focused PostgreSQL proof **PASS**; cumulative Gate 13 pending. |

## Security and attack coverage

The delivery assigns every required attack family to one implementation boundary: core scope/field/stale/migration attacks in P13.2–P13.3; pipeline/view/dashboard attacks in P13.4; malformed import, export, dedupe, and merge attacks in P13.5; provider/webhook/recipient attacks in P13.6; workflow execution/replay/privilege attacks in P13.7; report recipient, record/field, decimal, currency, and timezone attacks in P13.8; and predecessor/backup/restore/rollback/restart/inventory/attestation attacks in P13.9.

Notable retained denials include cross-application and cross-team non-enumeration, hidden money-field denial, forged owner/team/pipeline/stage/revision denial, stale CAS with zero business effects, closed CSV dialect/size/formula/protected-field admission, worker-fence replay isolation, no browser/domain provider secrets, no arbitrary workflow expression/network/fan-out, and custom-page ACL non-escalation. The P13.10 corpus machine-maps these proofs, but the clean exact-head cumulative execution remains pending.

Task creation now preallocates one server-owned positive-int32 ID, admits that ID only for the exact trusted request and transaction, and inserts the complete Task plus genesis audit in one atomic create. Replay allocates no new ID; malformed, aborted, raced, or failed requests commit no Task, audit, outbox, or idempotency effect. Static Green has only the narrow Task-create, sequence, idempotency, and outbox grants required by this path; it has no Task `UPDATE`/`DELETE` or authorization/lifecycle-state mutation privilege. Shared create locks and exclusive lifecycle/authorization locks establish one ordering across ordinary actions, scope changes, release, and rebind; the final static PostgreSQL proof exercises the corresponding race, denial, rollback, replay, and promotion matrix.

## Data, metrics, accessibility, and operations truth

- P13.10 seeds at least 100 Leads, 50 Accounts, 100 Contacts, 50 Opportunities across all six current stages, 200 Activities, and 100 Tasks in both the PostgreSQL evidence fixture and the generated-host performance fixture. Its real 10,000-row import/replay produces 10,000 unique targets, one receipt, bounded 250-row chunks, crash recovery, and a rejected-row diagnostic.
- P13.10 reconciles the seven fixed reports to an independent test-owned ledger. Exact-decimal money delta is zero and the `America/New_York` boundary fixture lands in the expected bucket.
- The generated-host proof runs representative and manager accounts through the pre-restore daily route matrix with zero serious/critical accessibility violations, then repeats dashboard, reports, and account-detail accessibility/authority smoke after physical restore.
- Declared controlled-CI profile: Apple M1 Max arm64, 10 logical CPUs, 64 GiB memory, concurrency 1, one cold browser navigation per route, and 20 warm server-observed samples after one untimed list/detail request. Observed server p95 is list **695.987 ms**, detail **843.943 ms**, action **539.149 ms** (limit 1,000 ms); browser readiness p95 is **1,227.12 ms** (limit 2,500 ms). The generated calendar proof uses Payload's exact date-range operators and paginates the 200 representative Activities through canonical 25-row pages. These are controlled-fixture beta checks, not production-capacity claims.
- P13.9 has a physical clean restore/restart fixture. It is not a customer RTO/RPO drill: no successful restore within four hours or observed recoverable-point age within 15 minutes is recorded here.

## Package, migration, and restore boundary

P13.9 verifies the exact registered Phase 12 migration prefix then applies the complete 33-entry registered migration chain, including the final static rebind lock-protocol upgrade. It snapshots full predecessor truth, proves canonical successor records and immutable migration evidence, checks current-v1 package/lock release-manifest authority, and restores a physical PostgreSQL backup into a clean distinct database. The test rejects every `down` path as `maintenance-required`, preserving the declared forward-only/restore rollback boundary rather than simulating a destructive rollback.

The generated-host restore proof authenticates the owner after restore, verifies health/inventory, dashboard, pipeline/Kanban, account activity/timeline/audit, current permission denial, and a completed report download. It exact-compares the report run/artifact/delivery IDs, bytes, artifact digest, metadata, execution digest, and watermark before and after restore; an unrelated viewer receives no CSV bytes.

## Pilot/dogfood evidence record and sign-off classification

| Field | Recorded value |
|---|---|
| Evidence class | `controlled-fixture` |
| Application release | `1.0.0` current-v1 package closure |
| Operators | Synthetic `fixture-persona:sales-representative` and `fixture-persona:sales-manager`; human operators: none |
| Dates | 2026-09-09 to 2026-09-09; consecutive human-operation business days: 0 |
| Support owner | Not assigned |
| Incidents | No human-operation incident record; Sev-1/Sev-2 closure not observed |
| Observed results | Technical controlled-fixture readiness passed; limited-beta operations not observed |

No human dogfood/external pilot record, two active human users, five consecutive business days, incident record, support owner, RTO/RPO observation, or product/Sales-engineering/security/operations sign-off is present. Automated fixture identities are test personas, not human pilot participants.

Therefore this result is intentionally **REWORK — fixture readiness only**. It must not be relabeled `GO LIMITED CRM BETA` until all ADR-0028 limited-beta acceptance conditions and every P13 kill criterion are evidenced at the exact release head.

## Pending Gate 13 evidence

The focused P13.10 PostgreSQL and generated-host tests pass locally. Cumulative run `34310642965` exposed stale pre-Phase-13 Sales integration fixtures and a root-barrel Lexical bundle regression in Gate 0; those failures were repaired and exact `pnpm phase:0` now passes. Hosted current-v1 attestation and deployment evidence was then regenerated from repaired source head `59db6f5` by release-evidence run `34313478146`, verified, and overlaid into this result head. Cumulative run `34314226406` confirmed the functional fixes and then exposed two bounded heavy tests exceeding their 5- and 15-second limits under full Turbo CI contention. Run `34314920576` showed the first test could also exhaust an interim 10-second bound under the same contention. Both retain every assertion with reviewed 30- and 60-second per-test limits. Run `34315458015` passed those points and exposed fixed 10-millisecond MessageChannel waits in the UI-runtime tests; independent contention reproduction confirmed test synchronization races rather than a product failure, and the affected assertions now await their exact mock invocations. Run `34316690197` passed Gate 0 and exposed a tracked Gate 1 resolution left stale by the Sales manifest changes; the official generator refreshed it, and the Gate 1 check plus isolated reproducibility proof pass. Run `34317780496` advanced through Gates 0–8, then exposed packed/static customer closure drift: Task creation still had a manual persistence path, required a post-create mutation unavailable to static Green, and lacked one global lock order across ordinary authorization use, static rebind, and release. The repaired implementation uses the canonical action gateway, atomic Task/genesis-audit insertion, one-shot trusted ID admission, and shared/exclusive advisory ordering. Focused Sales, composition, payload-adapter, real PostgreSQL migration/lifecycle/upgrade, generated-host, package-closure, and exact static deployment proofs pass. Exact-head cumulative run `34777004828` then reached the generated authorization executable suite but repeated the same emitted-module compile eight times and exceeded Vitest's default five-second limit under CI contention; the reviewed harness now loads once, resets all per-case state, and retains all eight scenarios and assertions. Exact-head hosted run `34777022225` exposed fresh factory-lock drift because Puck's open transitive range selected a newer TanStack virtual package on Linux. A relationship-scoped exact override preserves the reviewed version, and fresh Node 24.19 generation now produces identical locks on macOS and Linux. Hosted rerun `34777797403` passed on exact source `336b5c5` and supplied 33 verified evidence files. Cumulative run `34777795307` then exposed that emitted customer workspaces lacked the lock's override config, so their frozen installs correctly rejected the mismatch before runtime. The canonical workspace generator now emits the same scoped override, local and Linux locks converge, alpha/beta frozen installs and builds pass, previous-release upgrade passes, and packed customer boot reaches and passes runtime with the exact legitimate 17-migration inventory. Hosted run `34780018892` then passed on exact source `5246aaf` and supplied 33 verified evidence files. Cumulative run `34780011261` passed all 23 real-PostgreSQL customer suites, including the static matrix, then exposed Gate 2's pre-CRM opportunity benchmark mock: it returned Opportunity rows without the now-required active Pipeline and Stage projection. The updated benchmark fixture supplies canonical Pipeline/Stage records and opaque stage IDs, rejects unknown collections, and passes Gate 2 without weakening the production fail-closed projection check. Hosted run `34781378475` then passed on exact source `6529bdd` and supplied verified evidence. Cumulative run `34781368804` passed static/fixture evidence plus Gates 2, 2A, and 3, then exposed Gate 4's proof harness still naming an optional-field behavior replaced during Phase 13. The proof map now targets the current removed-prior-field denial, while the separate gateway-valid omitted-cell parity proof remains present; direct Gate 4 executes all 23 exact proofs and passes. Hosted run `34783212188` then passed on exact source `82138e1`. Cumulative run `34783204619` passed Gates 4 and 5, then exposed the Sales conformance manifest still naming the Phase 12 UI/Puck proof that allowed every block into Puck. The manifest now targets the current real proof that renders every UI contribution outside the editor while admitting only report-safe blocks to Puck; all 11 plugin-conformance proofs pass, and the official generator refreshed the Sales reference document from the current manifest. Hosted run `34785031495` then passed on exact source `76451fd`. Cumulative run `34785026690` passed Gate 6 and every Gate 7 package, browser, and performance proof, then exposed Gate 7's Phase 12 assertion that Sales still had seven Puck bridges. The gate now exact-compares the ordered closed Phase 13 inventory of six authorable work blocks and seven fixed report blocks, so additions, removals, and reorderings fail. Hosted run `34786767926` then passed on exact source `2992968`. Cumulative run `34786762845` passed Gate 7 and Gate 8 package, composition, runtime, and generated-evidence checks, then exposed Gate 8's Phase 12 assertion that a fresh current-v1 factory plan contained only Sales. The plan intentionally includes the exact Socket.IO realtime provider bound to `realtime.gateway`; Gate 8 now exact-compares the ordered two-plugin inventory, retaining fail-closed add/remove/substitution/order/version checks. Current exact-head cumulative and hosted reruns remain pending:

Hosted run `34788667362` then passed on exact source `4870d60`. Cumulative run `34788662328` reached Gate 10 and exposed five authorization fixtures still using pre-CRM revision, permission, Task seed, scope/settings, and update/race contracts. The fixtures now use current migration revision 25, published permissions, complete tenant/audit/scope/settings state, terminal Task CAS input, real scope exit, and durable outbox identity; the full Gate 10 PostgreSQL/Chromium suite passes 15/15 without changing production authorization.

Hosted run `34793036197` then passed on exact source `f22019c` and supplied the exact 33-file release-evidence closure. Cumulative run `34793035268` reached Gate 12 and exposed generated-app drift after the Phase 13 contract changes: readiness omitted the current Sales collection/migration inventory; route, action, Saved View, authority-revision, and authorable-Puck fixtures used retired shapes; a second clean generated application had no application-scoped `system.general` v3 row; and reminder-only worker operation incorrectly required a communication provider. The repair seeds the clean-install settings row transactionally and idempotently from the factory's explicit ISO primary currency, distinguishes reminder-only from provider-backed communication work, and refreshes the canonical generated closure.

The real Gate 12 path also exposed production defects. Internal Opportunity projection and mutation fences required pipeline-read authority even though the public policy permits Opportunity readers and mutators without pipeline administration; those internal reads now use the corresponding Opportunity read or mutation authority without exposing pipeline routes or detail. The server mutated registered Puck source descriptors by actor-filtering `outputFields`, invalidating their structural identity; descriptors now remain canonical while record and field enforcement stays in the projection/authority layers. Platform-only page validation and projection unnecessarily required an active Sales generation; dependency-free view/validation now uses the generic profile, while the editor admits Sales authoring only when a Sales generation exists and every Sales-dependent document remains fail-closed. Provider-unavailable workers skipped queued communications or could fail before reminder processing; provider validation is now lazy per claimed communication, so absent, empty, malformed, and non-loopback endpoints produce canonical first-attempt `HOST_INVARIANT` dead letters while reminders and notifications continue and no Activity success is recorded. Exact P12.9 real PostgreSQL/Chromium passes 1/1 in 399.72 seconds, including disabled-Sales generic editor/autosave and rejected Sales-node injection with an unchanged revision. Exact P12.10 generated minimal/neobrutalism real-PostgreSQL proof passes 1/1 in 192.20 seconds with bounded exact-child cleanup, and the provider-host PostgreSQL matrix passes 1/1. Composition passes 172/172, Sales passes 72/72 Node and 82/82 Vitest, and build, boundary, pack, frozen-install, official-generation stability, and diff checks pass. Persistent review passes; the exact-head cumulative rerun remains pending.

Exact-head cumulative run `34818769622` and hosted-evidence run `34818778307` then both failed during the pre-gate frozen install on source `9e9bc6a`: the newly deterministic Sales archive matched the alpha, beta, factory-lock, and release-manifest authorities, but the repository root lock still retained the prior archive integrity. The Node 24.19 official lock workflow refreshed exactly that root integrity entry. Root, alpha, and beta frozen installs now pass; all root/customer/factory lock consumers match the packed archive and release manifest; the full official generation sequence is stable; and `git diff --check` passes. No gate or hosted artifact ran on the failed head, so both exact-head workflows remain pending after this repair.

Cumulative run `34819383164` on exact source `ba2030b` reached Gate 1 and exposed one further derived closure: `.k-nex/generated/k-nex.resolved.json` retained the prior Sales archive integrity. The official Gate 1 generator refreshed exactly that field. Repeated generation and exact `check:gate-1` pass, the resolved graph matches the actual archive, packed ABI/release closure passes, and the root frozen install remains clean. Hosted-evidence run `34819395319` subsequently passed on the same source, produced exactly 33 declared tracked files, and passed byte, source-binding, manifest, generated-evidence, and diff checks. Persistent review passes; an exact-head cumulative rerun remains pending.

Cumulative run `34820186689` on exact source `56941fb` passed Gates 9–11, then Gate 12 exposed a stale proof-map marker: the gate required `DENIED` while the fixture and accepted authority contract prove field-level `REDACTED` behavior. The unchanged real fixture receives HTTP 200 and a successful source result, retains authorized rows, and exact-checks that the forbidden `amount` key is absent. Only the authoritative Gate 12 process-marker/proof-map expectations now require `REDACTED`; all required Gate 12 attack markers and the static/fixture closure pass. Hosted-evidence run `34820204219` subsequently passed on the same source, overlaid exactly the 33 declared paths, and passed byte, source-binding, manifest, scope, generated-evidence, and diff checks. Persistent review passes; an exact-head cumulative rerun remains pending.

Cumulative run `34826722037` on exact source `b9f34c2` passed Gates 9–11 and the corrected Gate 12 attack-marker admission, then the serialized P12.9 and P12.10 generated-application builds exceeded their shared hardcoded 120-second child-command bound under CI. The failure was `spawnSync pnpm ETIMEDOUT`, not a product assertion or outer-test timeout. A shared generated-application command helper now gives only exact `pnpm build` a finite 300-second bound while install, migration, doctor, bootstrap, OpenSSL, and every other command retain 120 seconds; Gate 12 remains serialized. The helper regression passes 1/1, exact P12.9 real PostgreSQL/HTTP/Chromium passes 1/1 in 397.45 seconds, exact P12.10 real PostgreSQL/theme proof passes 1/1 in 193.84 seconds, and their serial run passes 2/2 in 595.89 seconds. Hosted-evidence run `34826748152` subsequently passed on the same source with the exact 33-file overlay and clean binding/manifest/scope checks. Persistent review passes; an exact-head cumulative rerun remains pending.

Hosted-evidence run `34835176159` passed first on exact source `4967811`, with the exact 33-file overlay and clean binding, manifest, generated-evidence, scope, and diff checks. Cumulative run `34835178794` then passed Gates 0–12 before P13.3 returned `403 Sales current authority is unavailable`. The manual fixture lacked the accepted application-scoped `system.general` v3 settings row and still used retired pipeline/action shapes. The durable resolver also compared immutable generation-row creation revisions to later global RBAC revisions, so any legitimate post-generation role grant invalidated all Sales authority. Generation-row revisions are now captured and revalidated independently from global authorization/lifecycle revisions.

The same real PostgreSQL path exposed incomplete current stage semantics and detail projection, concurrent Payload reads on one transaction client, and direct durable-authority SQL outside the five-second fail-closed deadline. Opportunity update keeps UUID stage identity in persistence/audit while returning the existing v2 semantic output; detail projection loads and validates the exact active pipeline and locked stage under Opportunity read authority without granting pipeline administration; same-client reads are sequential; and durable resolve plus revalidate are bounded by the five-second authority deadline. Exact P13.3 passes 1/1 in 32.18 seconds, Sales server passes 42/42, CRM/pipeline passes 24/24, composition authoring passes 14/14, and builds, official source/package/lock/manifest generation, 17-package closure, Gate 1 stability, root/alpha/beta frozen installs, and diff checks pass. Persistent review passes; an exact-head cumulative rerun remains pending.

Cumulative run `34847441076` and hosted-evidence run `34847447704` then began concurrently on exact source `bd4c768`. The cumulative run passed Gates 0–7 and Gate 8 correctly rejected the still-committed pre-refresh evidence against the new P13.3 package/factory/release closure. The hosted run subsequently passed and supplied exactly 33 declared evidence files matching that closure. The reviewed overlay passes byte, source-binding, manifest, generated-evidence, and scope checks; exact Gate 8 including its real PostgreSQL restore passes; and the authoritative local source, double-pack, factory-lock, release-manifest, Gate 1, frozen-install, and diff checks reproduce with zero generated-input drift. This was workflow ordering, not a generator or product defect. The overlay must be committed before the next exact-head cumulative run.

After the reviewed overlay was committed, cumulative run `34851627469` on exact head `5547cff` passed Gates 0–11. P12.9 then timed out after 555.50 seconds while a late `/sales/tasks` navigation waited 30 seconds for Playwright's broad `load` event; no HTTP response, status, semantic route readiness, or server failure was observed by that assertion. Registered Sales-route navigation now uses a bounded helper that admits only the four exact registered Sales paths, rejects query, fragment, userinfo, and unregistered paths before navigation, waits up to 30 seconds for response commit, requires exact HTTP 200 plus exact response/page canonical URLs, then waits up to 30 seconds for the route-specific semantic locator, with a separately bounded body/process diagnostic on failure. It does not change global Playwright defaults or unrelated login, editor, theme, and published-page navigation contracts. The helper passes 4/4, the fixture suite passes 7/7, and exact P12.9 real PostgreSQL/Chromium passes 1/1 in 399.98 seconds through the failed target and final marker. Persistent review passes; an exact-head cumulative rerun remains pending.

Cumulative run `34863344938` on exact head `6bcebab` passed Gates 0–11, then exposed one remaining synchronization gap in that navigation proof: response commit and the server-rendered Task form did not prove React hydration or the generated client's immediate projection refresh and five-second loss-recovery poll had started. The server remained fail-closed and the projection endpoint maps revoked authority to 404, but the test could revoke before the client loop existed and exhaust its unchanged 10-second alert bound under cumulative compilation contention. Navigation now observes the exact route projection HTTP 200 before returning; the revocation and supported-disable proofs concurrently observe the exact recurring projection HTTP 404 and the unchanged ten-second unavailable-alert bound before asserting the cleared action. Focused helper tests pass 5/5, the fixture suite passes 8/8, and exact P12.9 real PostgreSQL/Chromium passes 1/1 in 406.42 seconds. No product, global browser, polling, or alert bound was widened; persistent review and the exact-head cumulative rerun remain pending.

Cumulative run `34873730246` on exact head `e0ef64f` passed Gates 1–11, then the initial projection-200 observer timed out while the exact `/sales/tasks` URL and its complete server-rendered Task UI remained visible. Source and log inspection excluded redirect, origin, query-order, service-worker, and non-200-response mismatches: the optional client refresh had not begun, so treating its response as generic navigation readiness merely moved the hydration race. The Task route now proves actual hydration without React internals or product-only markers by retrying a reversible title transition until React state enables Create task, then clearing the title and verifying the button is disabled again; it never submits or creates business data. Only after that interaction does the proof arm the exact projection 404, revoke authority, and concurrently require the unchanged ten-second unavailable alert. The same readiness proof protects the later supported-disable path. Focused helper tests pass 6/6, including an ignored pre-hydration event, the fixture suite passes 9/9, and exact P12.9 real PostgreSQL/Chromium passes 1/1 in 402.48 seconds. No product, polling, or timeout bound changed; persistent review and the exact-head cumulative rerun remain pending.

Cumulative run `34884393849` on exact head `7b4674e` passed Gates 1–11, then P12.9 logged transient `Sales current authority changed during permission projection` failures while the hydration probe exhausted its shared deadline and gave a final Title fill only two milliseconds. Awaiting the authority-restoration commit cannot prevent later concurrent revision changes, so generated actor construction now retries its complete read-only scope/authority/settings/generation snapshot, bounded permission projection, and final revision fence exactly once only for a private revision-drift error. A stable second snapshot returns one coherent current permission set; a revoked permission cannot leak from the discarded attempt; repeated drift and unrelated failures still fail closed. Action execution is not retried, and its transaction/CAS fences remain authoritative. The hydration proof keeps the real reversible Title-to-submit React state transition and one shared unchanged ten-second total bound, including the initial check and final restoration. It admits an attempt only while 5.1 seconds remain, retries only the real Playwright `TimeoutError`, caps each locator operation at one second, and fails before calling Playwright if no positive budget remains; non-timeout failures are immediate. A discarded local density-probe attempt failed in 321.67 seconds because the Tasks route is a `data-table`, then source inspection confirmed its density control is intentionally inert without `onViewStateChange`; that approach was removed. A subsequent 71.66-second exact run was deliberately interrupted before the target to incorporate timeout-only retry review. The final exact P12.9 real PostgreSQL/Chromium proof passes 1/1 in 408.20 seconds, including both hydration sites, unchanged ten-second revoke/disable alert convergence, exact projection 404, action clearing, and final evidence. Focused helper tests pass 9/9, including deterministic 9.9-second near-deadline completion/restoration and exhausted-between-operations coverage proving no `timeout: 0` reaches Playwright; focused generated authorization and emitted-source tests pass 29/29, and composition build passes. Persistent review and the exact-head cumulative rerun remain pending.

The official current-v1 generation sequence then refreshed the changed Composition archive, both content-addressed factory locks, the package release manifest, and the alpha/beta lock integrities. The current-v1 Sales release source and Gate 1 resolved graph were already byte-current, and the repository root lock did not consume the changed archive, so those generators produced no diff. A second complete official generation produced the identical repository-diff fingerprint `48eb308458ac1c338ced5c27d911cab87d7f651fe501d3e1da0c3e64b80f6d59`. Current-v1 source check, canonical-pack tests 2/2, packed ABI and 17-package closure, factory-lock check 2/2, release-manifest tests 4/4, Gate 1 check and reproducibility, exact packed Composition source parity, root/alpha/beta frozen installs, and `git diff --check` pass. Exactly eight generated paths changed: one Composition archive, two old and two replacement factory-lock paths, two customer locks, and one release manifest; all hosted evidence paths remain untouched. Because the package integrity, factory-lock digests, manifest, and source head changed, the source/closure must be reviewed, committed, and pushed before the hosted run; its exact 33-file result must then be overlaid, reviewed, committed, and pushed before Gate 8 or cumulative acceptance.

Hosted release-evidence run `34897020052` then passed in 5m04s on exact source `727586660dfb65c1d65e51c69deda8f74dcd7141`: frozen install, build, packed/factory input verification, both current customer bundles, five hosted attestations, hosted verification, runtime/fleet generation, and artifact upload all succeeded. The downloaded artifact contains exactly the 33 already-declared tracked evidence paths and no symlinks or special files; every overlaid byte matches the artifact. Hosted attestation/source/manifest binding, both customer bundle/receipt/inventory chains, fleet scope, neutral-history security, generated-evidence stability, and `git diff --check` pass. Persistent review passed with no findings, after which the overlay was committed and pushed.

The reviewed overlay was committed at `fdc1b275e02746906584e974a297edce0733be1a`. Cumulative run `34898082878` on that exact head passed Gates 0–11, then Gate 12 passed P12.5 and P12.10 while P12.9 exhausted the self-imposed ten-second React hydration probe despite retaining the complete server-rendered Task form. Gate 12 runs the three process proofs serially with isolated PostgreSQL databases, temporary roots, and ports, so P12.10 did not contend with P12.9. The sole buffered server authority-drift message could not identify the failing page: older open Sales-backed pages continue bounded polling during the same authorization transitions, and the fixture previously captured neither page-specific projection responses nor browser failures. The navigation helper now records at most 32 route-scoped monotonic events for only the exact projection URL, including status and validated `Server-Timing`; request failures, page errors, and error-console messages are represented only by bounded SHA-256 prefixes, and listeners detach on replacement, page close, or helper failure. Server commit and semantic readiness remain 30 seconds; React hydration/restoration now has its own 30-second total budget with positive locator operations capped at one second. The independent authority-revocation and supported-disable UI convergence bounds remain exactly ten seconds. Helper tests pass 11/11, including bounded/redacted telemetry and deterministic 29.9-second restoration, and exact P12.9 real PostgreSQL/Chromium passes 1/1 in 404.79 seconds through both unchanged revocation/disable proofs. This latest harness-only batch awaits persistent review and an exact-head cumulative rerun.

That reviewed harness batch was committed at `922eecb08a55a275a06a37a938ed9f4acef70f20`. Cumulative run `34906668475` on the exact head passed Gates 0–11, P12.5, and P12.10. P12.9 passed the earlier hydration and open-route revocation proof, continued through restart replay, query-budget recovery, and current-owner handoff, then Node terminated it at exactly `600002.95ms` before the later disable/operator markers. No helper assertion or route telemetry failure occurred; the test exhausted its outer aggregate ceiling. Only P12.9's Node test metadata now permits 900 seconds, the same finite ceiling already used by the generated Phase 13 and static-deployment heavyweight fixtures. The static guard fixes that aggregate ceiling at 900 seconds, forbids the retired 600-second value in P12.9, and retains all four exact ten-second authority-revocation/disable UI waits. Every child-command, HTTP, Playwright navigation, hydration, product, revocation, disable, process-stop, and cleanup bound is unchanged. The metadata-only repair does not require another focused seven-minute execution before review; helper/static proof passes 12/12 and an exact-head cumulative rerun remains required.

Cumulative run `34913057019` on exact head `4a6b088cd1e001a82b963b385fb3cfb46b5aea2b` confirmed the 900-second aggregate ceiling and passed Gates 0–11 plus P12.5/P12.10. P12.9 failed after 576.77 seconds when its isolated `/sales/tasks` hydration proof retained the complete server-rendered Task form but observed no React-owned Title transition, projection response, request failure, page error, or error console event. The fixture was keeping the owner Sales page plus a manager published page and editor alive across independent authority mutations; their one-second shell/page/editor polling kept concurrent projection attempts active across unrelated revision changes, while reusing the first manager page also allowed the later page-ACL proof to match its stale earlier `Page access revoked` alert. The repair closes the owner page after its route/action proof, closes the first manager page/editor pair immediately after their Sales-authority revocation proof, runs the registered-route revoke proof with the sole live manager page, then opens fresh manager view/editor surfaces only immediately before the separate page-ACL mutation. This removes unrelated pollers and makes the later alert/navigation assertions originate from the new mutation. No production, timeout, polling, authorization, or convergence contract changed. The focused lifecycle/static helper passes 13/13, and exact P12.9 real PostgreSQL/Chromium passes 1/1 in 376.64 seconds through every final marker. Persistent review and an exact-head cumulative rerun remain pending.

Cumulative run `34920349221` on exact head `d006e3e` passed Gates 1–11 and Gate 12's P12.5/P12.10 process proofs. P12.9 passed every browser lifecycle, revocation, recovery, response-loss, and durable replay boundary, then its crash-injected administration-operator restart failed because the operator's bootstrap-only 240-second worker fence had expired during the longer cumulative execution. The operator now retains the static deployment store and renews the same generation, token, owner, and promotion revision every 60 seconds through the existing bounded 240-second `renewWorkerFence` contract. Renewal is serialized and non-overlapping; shutdown and signals cancel the timer and await in-flight renewal before closing server and pool. Any stale, replaced, or expired authority marks the operator unavailable before shutdown, stops accepting commands, and exits non-zero without retrying or reclaiming the fence. Focused heartbeat tests pass 4/4, including a deterministic blocked-renewal shutdown race and the exact authority → heartbeat → HTTPS → PostgreSQL close order; exact P12.9 real PostgreSQL/Chromium passes 1/1 in 406.89 seconds, proves the lease remains live with unchanged identity across the long journey and three crash restarts, then replaces the owner and proves the stale operator fails closed without takeover. No lease maximum, lifecycle reconciliation, production authority boundary, or product timeout changed. Persistent review and an exact-head cumulative rerun remain pending.

```text
OBSERVED: P13.10 focused PostgreSQL and generated-host proofs
FAIL: cumulative run 34920349221 at Gate 12 P12.9 expired administration-operator worker fence
PASS: hosted current-v1 attestation refresh, run 34897020052, source 7275866
PASS: remediated exact static deployment PostgreSQL proof, 1/1, 358.92s
PENDING: reviewed administration-operator heartbeat commit and repaired exact-head Gate 0–13 result
OBSERVED: representative dataset/import ledger, seven-metric reconciliation,
          controlled performance profile, representative/manager accessibility, physical fixture restore
NOT OBSERVED: human-operation RTO/RPO drill and limited-beta sign-offs
```

**Current decision:** **REWORK — automated fixture readiness only.**
